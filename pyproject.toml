[project]
name = "expertarcher-goldenrecords"
version = "0.1.0"
description = "Prepare ExpertArcher scores for submission to the Golden Records Scores API."
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
